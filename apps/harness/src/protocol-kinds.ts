// Provisional kind numbers — see protocol/trace-provenance.md. Not yet
// registered as a real NIP; keep these in sync with that document.
export const FILE_TRACE_NODE_KIND = 4290;
export const FOLDER_MANIFEST_KIND = 34290;
